fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
python-dotenv==1.2.2

# LLM / agent stack
langchain-core==1.4.8
langchain-openai==1.3.3
langgraph==1.2.7
langgraph-checkpoint-postgres==3.1.0

# Postgres
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1

# Document generation / parsing
python-docx==1.2.0
pypdf==6.14.2

# JD scraping — this exact version is what `playwright install chromium`
# in the Dockerfile fetches the matching browser build for.
playwright==1.61.0

# Payments / auth / storage
razorpay==2.0.1
google-auth==2.55.2
boto3==1.43.43
botocore==1.43.43

# Required for FastAPI's UploadFile parsing (resume uploads) — not directly
# imported anywhere, but the app will fail at runtime without it.
python-multipart==0.0.32